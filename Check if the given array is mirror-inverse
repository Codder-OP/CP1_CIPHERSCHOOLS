#include <iostream>
using namespace std;

int main() {
	int n;
	cin>>n;
	int ar[n];
	for(int i=0;i<n;i++)
	    cin>>ar[i];
	bool f=true;
	for(int i=0;i<n;i++)
	{
	    if(ar[ar[i]]!=i)
	    {
	        f=false;
	        break;
	    }
	}
	if(f)
	    cout<<"Yes";
	else
	    cout<<"No";
	return 0;
}
